add_newline = true
command_timeout = 2000

format = """$username @ $hostname
$directory $git_branch $git_status
$character
"""

[character]
success_symbol = "╰⎯"
error_symbol = "╰⎯"

[env_var]
symbol = "╭╴"
variable = 'SHELL'
format = "$symbol"
disabled = false

[username]
style_user = 'black bold'
style_root = 'black bold'
format = '(black bold)[$user](bold white)'
disabled = false 
show_always = true

[hostname]
ssh_only = false
format = '(black bold)[$hostname](bold white)'
disabled = false

[directory]
truncation_length = 30
truncation_symbol = '…/'
home_symbol = '~'
read_only_style = '197'
read_only = '  '
format = '(black bold)[$path]($style)[$read_only]($read_only_style)'
style = 'bold white'

[git_branch]
symbol = ''
format = '(black bold)[$symbol\[$branch\]]($style)'
truncation_length = 30
truncation_symbol = '…/'
style = 'bold white'

[git_status]
disabled = true
format = '[$all_status $ahead_behind]($style)'
style = 'bold green'
conflicted = '!!!'
up_to_date = ''
untracked = ' '
ahead = '⇡${count}'
diverged = '⇕⇡${ahead_count}⇣${behind_count}'
behind = '⇣${count}'
stashed = ' '
modified = ' '
staged = '[++\($count\)](green)'
renamed = '襁 '
deleted = ' '

[nodejs]
disabled = true
